rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /slideshow/{allPaths=**} {
      allow read: if true;
      // BUG FIX: restrict write to authenticated users only (was open to anyone)
      allow write: if request.auth != null;
    }
  }
}
